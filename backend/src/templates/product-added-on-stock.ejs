<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Votre produit préféré rajouté en stock sur LAYALIN !</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            color: #393939;
        }
        .product-resume {
            text-align: center;
        }
        .informations {
            background: #dedede;
            padding: 1rem;
        }
        .product-name {
            font-size: 1.5rem;
            margin: 0;
        }
        .description {
            color: #7f8c8d;
        }
        img {
            max-width: 200px;
            border-radius: 8px;
            margin: 10px 0;
        }
        .url {
            display: block;
            text-align: center;
            text-decoration: none;
            color: #172822;
        }
    </style>
</head>
<body>
<div class="container">
    <header style="background: #172822; color: white; padding: 1rem 0; text-align: center">
        <h1>Restock de votre produit préféré !</h1>
    </header>
    <div class="product-resume">
        <img src="<%= product.thumbnail %>" alt="<%= product.name %>">
        <h1 class="product-name"><%= product.name %></h1>
        <p class="description"><%= product.description %></p>
    </div>
    <div class="informations">
        <p class="">Prix: <b><%= product.price %> €</b></p>
        <p class="">Marque: <%= product.brand %></p>
        <p>Poid: <%= product.weight %> g</p>
        <p>
            Catégorie:
            <% product.categories.forEach(category => { %>
                <span><%= category %></span>
            <% }) %>
        </p>
        <p>
            Couleur:
            <% product.colors.forEach(color => { %>
                <span><%= color %></span>
            <% }) %>
        </p>
        <p>
            Matériaux:
            <% product.materials.forEach(material => { %>
                <span><%= material %></span>
            <% }) %>
        </p>
    </div>
    <div style="padding: 20px 0">
        <a class="url" href="<%= productUrl %>" target="_blank">Voir le produit</a>
    </div>
    <footer style="background: #dedede; text-align: center; padding: 1rem 0">
        <small>© 2024 Layalin. Tous droits réservés.</small>
    </footer>
</div>
</body>
</html>
